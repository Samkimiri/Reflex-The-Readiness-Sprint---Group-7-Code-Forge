node_modules/
backend/data/db.json
.DS_Store
*.log

.vercel
.env*.local

# Local Claude Code skill installs (e.g. ui-ux-pro-max) — tooling config
# for the assistant, not part of the app; no reason to ship it in the repo.
.claude/

.vercel
.env*.local
